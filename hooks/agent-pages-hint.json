{
  "_comment": "Optional. Nudges the assistant to offer /agent-pages when a standalone HTML artifact would be clearer than a long Markdown response. Merge this into the UserPromptSubmit array of ~/.claude/settings.json, or run scripts/install.sh --with-hook to do it for you.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cat >/dev/null 2>&1; printf '%s' '{\"hookSpecificOutput\": {\"hookEventName\": \"UserPromptSubmit\", \"additionalContext\": \"When the user'\"'\"'s request would be easier to understand as a self-contained HTML artifact than as a long Markdown response, briefly offer `/agent-pages <topic>` and ask whether they want that format. Good fits include side-by-side exploration or planning, annotated code review, design systems or component variants, interactive prototypes, diagrams and research maps, slide/report-style communication, incident/status/PR write-ups, or small custom review/editing UIs. Do not suggest it for quick answers, short explanations, routine edits, or when the user clearly wants plain text.\"}}'",
            "timeout": 5,
            "statusMessage": "Inject /agent-pages design hint"
          }
        ]
      }
    ]
  }
}
